# 应用服务 WEB 访问端口
server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/student?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: root

  data:
    redis:
      host: localhost
      port: 6379
      password: redis

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.swagger3.model